#App
REACT_APP_API="${APPSYNC_API}"
REACT_APP_ID_POOL="${COGNITO_ID_POOL}"
REACT_APP_USER_POOL="${COGNITO_USER_POOL}"
REACT_APP_WEB_CLIENT_ID="${COGNITO_CLIENT_ID}"
REACT_APP_COGNITO_DOMAIN="${COGNITO_DOMAIN}"
REACT_APP_SIGNIN_URL="${SIGNIN_URL}"
REACT_APP_SIGNOUT_URL="${SIGNOUT_URL}"
REACT_APP_MAIN_WEBSITE_SIGNOUT_URL="${MAIN_WEBSITE_SIGNOUT_URL}"